# Samples

Put MPEG-TS files here for quick local testing, or change `data/settings.json` to point at your real library.

For process-control testing without a real stream, set:

```json
"smootherCommand": "./scripts/fake_smoother.mjs"
```
